#include <nes\cartridge\mappers\mapper_001.h>

namespace nes {

  BYTE Mapper001::read(WORD address, Cartridge * cartridge) {
    return BYTE();
  }

  void Mapper001::write(WORD address, BYTE value, Cartridge * cartridge) {
  }

}